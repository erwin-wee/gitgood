name: Test

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      # Debian/Ubuntu's xvfb package ships xvfb-run; scripts/smoke.mjs picks it
      # up automatically (and only uses it on Linux). Windows runners already
      # have a desktop session, so nothing extra is needed there.
      - name: Install xvfb
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y xvfb

      - name: Typecheck
        run: npm run typecheck

      - name: Unit tests
        run: npm run test:unit

      # Linux only for now. The fixture and smoke suites had never run on Windows
      # (the unit step failed first and masked them); when they finally did, 16
      # fixture tests failed for unrelated reasons -- cherry-pick conflicts,
      # worktrees, signing and the gh-stub. Typecheck and the unit suite still
      # run on Windows, which is what covers the Windows-specific code paths.
      # Tracked in #3.
      - name: Fixture tests
        if: runner.os == 'Linux'
        run: npm run test:fixture

      - name: Smoke tests
        if: runner.os == 'Linux'
        run: npm run test:smoke

      - name: Upload smoke artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: smoke-${{ matrix.os }}
          path: test/smoke/out/
          if-no-files-found: ignore
          retention-days: 7
