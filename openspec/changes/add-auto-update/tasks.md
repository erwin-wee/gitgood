# Tasks

## 1. Dependency review

- [x] 1.1 Run the new-dependency review for `electron-updater` (maintainer, downloads, licence, transitive deps, supply chain) and record the outcome and approval link in this change's design.md; verify the record exists before any `package.json` change
  - 2026-09-18: reviewed via `engineering:new-dependency-review` — active (last commit 2026-09-17), MIT, 14.6k stars, near-zero incremental transitive surface (most deps already pulled in by the `electron-builder` devDependency). Recommended approve; explicitly approved by the user for `electron-updater@6.8.9`.
- [x] 1.2 On approval, add `electron-updater` and the `publish` block to `electron-builder.yml`; verify `npm install` and `npm run build` succeed and the build emits `latest*.yml`
  - `package.json` now pins `electron-updater@6.8.9`; `electron-builder.yml` has `publish: { provider: github, owner: erwin-wee, repo: gitgood, draft: true }`. `npm install` (5 packages added) and `npm run typecheck`/`npm run build` pass. Emitting `latest*.yml` requires an actual `--publish` build, which needs a real GitHub token and a tag (covered by `.github/workflows/release.yml`, added alongside this task) — not re-verified locally here.
  - 2026-09-18 (later): `electron-updater`'s `autoUpdater` is now wired in — see 3.1/3.2 below. `github-provider.ts` (the dependency-free fallback) is no longer referenced from `src/main/index.ts`; it and its tests (`test/update-provider.test.ts`) are left in the tree, unused, rather than deleted in this pass.

## 2. Shared contract and settings

- [x] 2.1 Add `UpdateState`, the `app.update.*` methods and `app.update.changed` event, the three settings with defaults and `dismissedUpdateVersion`; verify `npm run typecheck`
  - `src/shared/types.ts` (`UpdateState`, `UpdateChannel`, `checkForUpdatesAutomatically`/`autoDownloadUpdates`/`updateChannel` settings), `src/shared/ipc.ts` (`app.update.*` methods, `app.update.changed` event), `src/main/store.ts` (`dismissedUpdateVersion` in `AppStateFile`). `npm run typecheck` passes.

## 3. Main process

- [x] 3.1 Implement the updater module: launch and 6-hour checks honouring the setting, channel/prerelease filter, no downgrades, state reducer emitting `app.update.changed`, progress forwarding; verify reducer and filter unit tests
  - `src/main/update/updater.ts` (launch + 6-hour checks, gated on `checkForUpdatesAutomatically`, manual checks always run), `src/main/update/update-core.ts` (`compareVersions`/`isNewerVersion`, `isEligibleForChannel`, `reduceUpdateState`), `src/main/update/electron-updater-provider.ts` and `provider.ts` (the `UpdateProvider` seam, now with `startDownload`/`quitAndInstall`). Unit tests: `test/update-core.test.ts`, `test/fixture/updater.test.ts`, `test/electron-updater-provider.test.ts`.
  - 2026-09-18 (later): real background download is wired in. `UpdateState`'s `downloading`/`ready` variants are live (carry the full release info, not just version), driven by new `download-start`/`download-progress`/`download-complete`/`download-error` reducer events; `Updater.startDownload()` orchestrates it and auto-triggers once an update is found when `autoDownloadUpdates` is on (unit-tested in `test/fixture/updater.test.ts`). Progress is reported directly on `UpdateState`/`app.update.changed`, not the repo-scoped `progress` IPC event this task originally named — that event is keyed by `repoPath`, which doesn't fit a global concept like an app update; documented as a deliberate deviation from the original design.md wording.
- [x] 3.2 Implement the install gate (operation in progress, AI active), current-repository persistence before quit, per-machine Windows fallback, and disabled state for unpackaged/portable/unsigned-macOS/read-only AppImage; verify gating unit tests and a smoke run reporting *Updates unavailable in this build* when unpackaged
  - The install gate (`canInstall` in `update-core.ts`, unit-tested for merge/rebase/cherry-pick/revert and AI-active), current-repository persistence in the `app.update.install` handler (`src/main/ipc.ts`), and disabled-state detection for all four cases (`detectDisabledReason`, unit-tested) — confirmed live in a smoke run (`test/smoke/scenarios/20-update-banner.json`, `about-disabled.json`/`.png`: real bootstrap state is `disabled` with the exact text *Updates unavailable in this build*).
  - 2026-09-18 (later): per-machine Windows detection is implemented (`isPerMachineInstall` in `update-core.ts` — true when the running executable path is under Program Files) and unit-tested (`test/update-core.test.ts`, `test/fixture/updater.test.ts`'s `quitAndInstall` case). `Updater.quitAndInstall()` passes `isSilent = !isPerMachineInstall` to the provider, which `ElectronUpdaterProvider` forwards to `autoUpdater.quitAndInstall(isSilent, true)`. Not verified against a real per-machine NSIS install (that needs a real Windows install of each kind — see 5.1).
- [x] 3.3 Log updater events through `logger.ts` without tokens; verify log lines in a smoke run
  - `Updater` logs the disabled reason on `start()` and each check's start/result/error via `log.info`/`log.warn` (never anything from `gh` output beyond a plain error message, never a token). Verified manually with `GITGOOD_USER_DATA` under `/tmp`: `Updater disabled: Updates unavailable in this build (development / unpackaged).` appears in `gitgood.log` on an unpackaged run (which is every dev/smoke run).

## 4. Renderer

- [x] 4.1 Build the ready banner (Restart, Release notes, Later) in `Banners.tsx` at lowest priority, the About section with version, channel and Check now, and Options → Advanced toggles; verify with an injected `UpdateState` via `actions.setUpdateState`
  - `src/renderer/src/components/Banners.tsx` (banner, lowest priority, shown even with no repository open), `src/renderer/src/components/dialogs/SettingsDialog.tsx` (`AboutDialog`'s channel/status/Check now, `AdvancedTab`'s Updates section, `UpdateNotesDialog`), `src/renderer/src/state/actions.ts` (`setUpdateState` exposed on `window.__gitgood.actions`, `checkForUpdates`, `downloadUpdate`, `installUpdate`, `dismissUpdate`, `openUpdateNotes`). Verified with an injected `UpdateState` in `test/smoke/scenarios/20-update-banner.json` (for the original `available` state only — see below).
  - 2026-09-18 (later): the banner and About-dialog status line now render `downloading` (spinner + percent) and `ready` (**Restart to update**, wired to `actions.installUpdate()` → `app.update.install`) too, now that a real provider reaches those states. Not re-verified in a smoke run in this pass — this sandbox has no X server/`xvfb-run` at all (not even the CI-style virtual one), so `npm run test:smoke` cannot run here; `npm run typecheck` and the full non-smoke `npm test` suite pass.
- [x] 4.2 Add Help → Check for updates… in `src/main/menu.ts` with up-to-date and error messaging; verify the menu action reaches the renderer
  - `src/main/menu.ts` (Help menu item, action id `check-for-updates`), `src/renderer/src/state/actions.ts`'s `handleMenuAction` and `checkForUpdates` (toasts for up-to-date/error/disabled). Verified by `npm run typecheck` and the existing `menu.action` event plumbing (same mechanism as every other Help item); not covered by a dedicated smoke step since none of the existing Help-menu items are either.

## 5. Verification

- [ ] 5.1 Build `0.1.1` and `0.1.2` to a test release repository and run `0.1.1` with `dev-app-update.yml`; verify detection, download, banner, install and reopen on the same repository on Windows and Linux runners
  - 2026-09-18: `electron-updater` is now wired in (see 3.1/3.2), but this task itself still needs a real release repository, a real GitHub account, and Windows/Linux machines or runners to actually install and relaunch a built app — none of which exist in this sandbox. Genuinely blocked on cutting real tagged releases via `.github/workflows/release.yml` and testing against them by hand; left unchecked.
- [ ] 5.2 Verify *Later* hides the banner until relaunch and that installation is blocked during a rebase
  - *Later* hides the banner: verified in the smoke scenario (`dismissed.json`: `bannerGone: true`) and unit-tested (`reduceUpdateState`'s `dismiss` case in `test/update-core.test.ts`); "does not return until the app is launched again" is by design (`Updater` never reads `dismissedUpdateVersion` back from disk on startup — see the comment on `Store.AppStateFile.dismissedUpdateVersion`).
  - Left unchecked: "installation is blocked during a rebase" is verified at the pure-gate level (`canInstall({ operationKind: 'rebase', ... })` in `test/update-core.test.ts`) and at the `Updater.quitAndInstall()`/IPC-handler level indirectly (the gate call is unconditional in `app.update.install`), but there is still no end-to-end UI test that starts a real rebase, gets a real update to `ready`, and confirms the Restart button is refused — that needs 5.1's real release pair (or a real rebase fixture wired into a smoke scenario) to exercise honestly.
- [x] 5.3 Run `npm run typecheck` and `npm test`; verify both pass
  - `npm run typecheck`: 0 errors. `npm test`: 43 files, 472 passed, 2 skipped (0 failed) — includes 52 new tests across `test/update-core.test.ts`, `test/update-provider.test.ts`, `test/fixture/updater.test.ts`, `test/fixture/update-provider-gh.test.ts`.
  - 2026-09-18 (later): re-run after wiring `electron-updater` in. `npm run typecheck`: 0 errors. `npm test`: 70 files, 904 passed, 2 skipped (0 failed) — adds `test/electron-updater-provider.test.ts` and new cases in `test/update-core.test.ts`/`test/fixture/updater.test.ts` for the download/install lifecycle and per-machine detection. `npm run build` also verified.
- [x] 5.4 Smoke screenshot of the banner and About dialog with an injected state; update README; verify
  - `test/smoke/scenarios/20-update-banner.json`: real About-dialog "Updates unavailable" state, an injected available-update banner (Download/Release notes/Later), the release-notes dialog, and Later hiding the banner — all with real screenshots (`test/smoke/out/update-banner/*.png`). `README.md`'s Features section updated. Full smoke suite (20/20) still passes.
  - Not re-run in this pass (see 4.1's note): this sandbox has no display server at all, so `npm run test:smoke` cannot execute here. The scenario itself wasn't edited, but it only ever exercised the `available`/`disabled` states, not the new `downloading`/`ready` ones — re-running it (or extending it) on a machine with a display is follow-up work before relying on the smoke suite as coverage for this change.
